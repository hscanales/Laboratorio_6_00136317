
function ejercicio9(num) {
    var array = [];
    for(let i=0; i<20; i++) {
        array.push(Math.floor(Math.random()*100));
    }
    console.log(array);
    if(arr.includes(num)) {
        console.log("Usted es brujo");
        return;
    }else{
    console.log("Usted es malo adivinando");
    }
}